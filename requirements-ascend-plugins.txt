# Installed with --no-deps after the complete official CANN 8.5.1 runtime is present.
# See requirements-ascend.txt and scripts/install_ascend.sh for why resolver
# metadata must not replace the validated Ascend dependency set with CUDA packages.
vllm==0.18.0
vllm-ascend==0.18.0
