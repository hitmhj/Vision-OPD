# Vision-OPD runtime for Ascend 910B / Atlas A2.
#
# This is intentionally separate from requirements.txt, which is a CUDA
# environment freeze.  vLLM-Ascend 0.18.0 requires the CANN/NNAL 9.0 line and
# the matching torch 2.9 ABI.  Never mix this file with the LLaMAFactory sample
# stack (CANN 8.5.1 / torch 2.10).
--extra-index-url https://download.pytorch.org/whl/cpu
--extra-index-url https://mirrors.huaweicloud.com/ascend/repos/pypi

# Binary core: upgrade or downgrade these packages as one unit only.
torch==2.9.0
torchvision==0.24.0
torch-npu==2.9.0.post2
torchdata==0.11.0
triton-ascend==3.2.1
vllm==0.18.0
vllm-ascend==0.18.0

# Vision-OPD / verl direct runtime dependencies.  LLaMAFactory-only packages
# such as trl, gradio, deepspeed and rouge_chinese are deliberately excluded.
accelerate==1.11.0
cloudpickle==3.1.2
codetiming==1.4.0
datasets==3.4.1
einops==0.8.2
filelock==3.24.3
hydra-core==1.3.2
numpy==1.26.4
omegaconf==2.3.0
orjson==3.11.7
packaging==26.0
pandas==2.3.3
peft==0.18.1
pillow==11.3.0
psutil==7.1.3
pyarrow==23.0.1
pyzmq==27.1.0
qwen-vl-utils==0.0.14
ray[default]==2.53.0
safetensors==0.7.0
scipy==1.12.0
sentencepiece==0.2.1
setproctitle==1.3.7
tensorboard==2.20.0
tensordict==0.10.0
tokenizers==0.22.2
tqdm==4.67.1
transformers==5.2.0
typing_extensions==4.15.0
