# Ascend 910B / cp311-aarch64 runtime requirements.
# torch, torch-npu, torchvision, CANN and HCCL intentionally come from the
# target image and must not be installed through this file.
-r requirements-ascend.txt
